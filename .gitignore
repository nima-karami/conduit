node_modules/
out/
dist/
*.vsix
.vscode-test/
*.log
.playwright/
.playwright-cli/
.playwright-mcp/

# Pencil design exploration (local only for now)
designs/

# Editor/IDE local config (never commit)
.cursor/
.idea/

# Autonomous build-loop machine run-state (transient, never commit)
.autoloop/

# Per-project Conduit runtime state (board/architecture/specs) — never commit
.conduit/

# Claude Code local state (incl. parallel-agent worktrees)
.claude/
