# Run ledger — source of truth. Never mark complete from memory.
run: 2026-09-18-html-viewer
conductor: opus-5-1m
mode: autonomous

phase0:
  status: done
  gate: "npm run verify"
  baseline_exit_before: 1      # red at ca4b92e — npm audit, js-yaml high advisory
  baseline_exit_after: 0       # green at f00a90c
  grounding_commit: f00a90c
  grounding_notes:
    - "js-yaml < 4.3.2 (GHSA-2883-xcg3-v3hh, CVSS 7.5) via electron-builder/electron-updater.
       npm audit fix -> 4.3.2, @vitest/mocker 4.1.11. Lockfile only."
    - "2 moderates remain (dompurify via monaco-editor); need a monaco major, do not trip
       --audit-level=high. Left alone deliberately."
    - "RISK-ACCEPTED: vitest testTimeout 5s -> 20s. Six unit suites shell out to real git;
       under parallel file execution a 0.6s suite took 5.07s and timed out. Liveness bound,
       not coverage — no assertion changed. Auditable here and in the run report."
  runtime_harness:
    - "node test/e2e/run-smoke.mjs <scenario> — real Electron app, hidden (CONDUIT_E2E=1)"
    - "npm run shots — theme screenshots"
    - "scratchpad probe on test/e2e/harness.mjs — used for the spec's measured baseline"

measurements:
  source: "scratchpad html-baseline-probe.mjs, real app, 2026-09-18"
  M1: "report.html -> {monaco:true, viewerControls:false, webviewFrame:false, docpageClass:false}"
  M2: "readme.md -> {monaco:false, viewerControls:true, controlLabels:['View source'], docpageClass:true}"
  M3: "html tab menu -> Keep Open|Close|Close others|Close to the right|Close to the left|Close all|Copy path|Copy name|Reveal in Explorer|Open in browser"
  M4: "web prompt + file:// -> {modalStillOpen:true, webviewFrames:0} (silent refusal)"

items:
  - id: html-viewer
    title: "View HTML documents inside Conduit"
    status: in_progress
    tier: FULL
    feature_type: UI
    spec: docs/specs/2026-09-18-html-document-viewing.md
    spec_decisions_needed: "8 (highest: high x2 — scripts execute w/ network blocked; guest scheme invariant widened)"
    spec_revision: 2
    plan: docs/plans/2026-09-18-html-document-viewing.plan.md
    topology: "solo — 7 shared entry files; no safe parallel lanes"
    build_sha: null
    gate_exit: null
    runtime_proof: null
    review: null
    qa: null
    notes:
      - "Phase 1 spec written; INDEX.md row added; reviewer subagent dispatched."
      - "Architecture (conductor call, non-delegable): conduit-preview: privileged scheme +
         hardened <webview> guest. NOT file:// (would move confinement to the renderer) and
         NOT sanitized inline HTML (strips scripts, cannot resolve relative assets, CSP has
         no frame-src)."

  - id: paste-truncation
    title: "Large multi-line paste into a terminal loses its tail"
    status: investigating
    tier: null
    added: "2026-09-18, by the user mid-run"
    report: |
      User pastes a page-long nested Markdown to-do list (copied from Notion) into a
      Conduit terminal running Claude Code. Previously a big paste was compacted/
      summarised in the UI but arrived intact; now the tail is dropped. User does not
      know whether the culprit is Conduit or Claude Code — that is the first thing to
      isolate, and it is isolable: paste the same payload into a plain `cmd.exe`
      session in Conduit and see whether the shell receives every byte. If it does,
      the bug is above Conduit; if it does not, it is Conduit's paste path.
    hypotheses:
      - "Unchunked write() to the ConPTY input pipe — a single large write can drop
         bytes past the pipe buffer. Highest prior."
      - "A cap/slice in the renderer paste path."
      - "Bracketed-paste not active, so embedded newlines submit early and the rest is
         eaten by whatever the first line launched."
    notes:
      - "Investigator dispatched. Decisive measurement designed: e2e harness paste into
         cmd.exe, compare bytes in vs bytes echoed."
      - "MEASURED: installed Claude Code is 2.1.274 (`claude --version`), at
         C:/Users/karam/.local/bin/claude.exe. A research agent attributed large-paste
         truncation to a 2.1.111-2.1.112 regression and cited issue numbers; that version
         range does not apply here and the citations are UNVERIFIED. Treat as a lead only."
      - "Actionable regardless of where the blame lands: a large paste should leave Conduit
         as ONE bracketed-paste envelope, in UTF-8-boundary-safe chunks, with small
         inter-chunk gaps. If Conduit currently issues a single large write to ConPTY, that
         is the likely cause and the fix is clear."
