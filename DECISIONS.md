# Overnight decision log

Autonomous build decisions, newest last. Review/override anything here.
Policy: **decide & document** — reasonable call at each fork, keep building.

| # | When | Fork | Decision | Reasoning |
|---|------|------|----------|-----------|
| 1 | 2026-06-08 | Project layout | Single extension package at repo root | Simplest; no monorepo needed for one extension |
| 2 | 2026-06-08 | Webview framework | React + esbuild | Stated default in design; fast bundling |
| 3 | 2026-06-08 | Unit test runner | Vitest for pure modules; @vscode/test-electron for integration | Vitest is fast and needs no VS Code; keeps TDD loop tight |
| 4 | 2026-06-08 | Full-window delivery | v1 opens dashboard as editor-area webview; user drags to own window. Auto-open in a dedicated auxiliary window deferred to v1.1 | Stable API has no direct "open webview in aux window"; editor webview + native "Move into New Window" gets the result now without proposed APIs |
| 5 | 2026-06-08 | Execution mode | Inline self-driven overnight, commit per task, screenshot webview via playwright-cli | Continuity + my own visual verification; subagents not needed for this size |
| 6 | 2026-06-08 | Webview verification | Render built bundle with mock state to temp HTML, screenshot with playwright-cli | Can't run a real VS Code webview headless cheaply; this verifies the UI itself |
| 7 | 2026-06-08 | @types/vscode + engine | Pinned both to ^1.120.0 (latest published types) | @types/vscode 1.123 not on npm yet; installed VS Code 1.123 satisfies ^1.120.0 |
| 8 | 2026-06-08 | Webview placeholder at Task 0 | Added minimal webview/index.tsx stub so `npm run build` is green before Task 9 | esbuild builds both bundles; Task 9 overwrites the stub with the real React entry |
| 9 | 2026-06-08 | npm audit | 5 vulns (dev deps) left as-is overnight | `npm audit fix --force` risks breaking changes; flag for morning review |
| 10 | 2026-06-08 | Activation events | Added `"activationEvents": ["onStartupFinished"]` | Integration test caught that the command was never registered without it — real bug, not just a test artifact |
| 11 | 2026-06-08 | Integration test robustness | Test awaits `extensions.getExtension('nima.agent-deck').activate()` before asserting | Avoids race between extension activation and the assertion |
| 12 | 2026-06-08 | Full-window UX | On open, auto-run `workbench.action.moveEditorToNewWindow` (setting `agentDeck.openInNewWindow`, default true, try/catch) | Directly serves the "new window like Agents Window" goal without proposed APIs. NEEDS VISUAL CONFIRMATION in the morning — couldn't verify an OS window overnight |
| 13 | 2026-06-08 | Line endings | Added `.gitattributes` (`* text=auto eol=lf`) + renormalized | Stops the CRLF-normalization warnings; consistent LF in repo |
| 14 | 2026-06-08 | Closed persistence wiring gap | Wired persistence into extension (restore-on-activate as stale, save-on-change) + added `SessionManager.restore`/`relaunch` + a "↻ Relaunch" button on stale rows | The plan created the persistence module + tests but never wired it; v1 scope required it. Now unit-tested (15 total) and visually verified |
| 15 | 2026-06-08 | Closed rename UI gap | Added inline rename (✎ button → editable field, Enter/blur commits, Esc cancels) | rename was wired in manager/protocol/panel but had no UI trigger; now the full v1 set (launch/focus/rename/kill/relaunch) is reachable. Visually verified |
| 16 | 2026-06-09 | **Major UI pivot** | After Nima saw v1, the minimal launcher was rejected. Target is now a full 3-pane Claude-Code-Desktop-style IDE shell (sessions sidebar + customizations, center conversation + composer, right Changes/Files). Decisions: VS Code extension webview (not standalone), "strongly inspired" (not pixel clone), visual shell with mock data first | The original text-only design under-specified the visual target; reference screenshots in docs/inspo/ reset it. v1 logic (registry/session/persistence) is reused; the webview UI was rebuilt |
| 17 | 2026-06-09 | Aesthetic + fonts | Refined dark IDE look, coral accent; Bricolage Grotesque (display), Hanken Grotesk (UI), JetBrains Mono (code/paths/diff). Loaded via Google Fonts; CSP updated to allow fonts.googleapis.com/gstatic.com | Distinctive but professional, true to the reference; per frontend-design guidance |
| 18 | 2026-06-09 | Shell milestone scope | Built sidebar + conversation + composer + Changes/Files as a visual shell on mock data. Floating Monaco-style editor overlay deferred to next pass | Get the look approved before wiring real data/functionality |
| 19 | 2026-06-09 | **Center = real embedded terminal** | Nima clarified the center must run the actual claude CLI/TUI, not a chat. Replaced the conversation view with an embedded terminal: xterm.js (@xterm/xterm + addon-fit) in the webview ↔ node-pty in the extension host, piped over postMessage | The chat view mimicked the Copilot/Claude extension; the real goal is the agent terminal. This **reverses decision #** (native terminals) — native terminals can't live in a webview — and re-grants per-session terminal theming. node-pty is external in esbuild; verified loading in the VS Code Electron host via integration test |
| 20 | 2026-06-09 | Preview fake-shell | The browser preview omits acquireVsCodeApi so the bridge falls back to a tiny fake shell (banner + echo), making the terminal visible in screenshots; real PTY used inside VS Code | Lets playwright-cli verify the terminal UI without an extension host |
| 21 | 2026-06-09 | (1) Per-session agent launch | term:start now carries agentId+cwd; host resolves via registry (`resolveLaunchSpec`) and launches that agent in the session's folder, shell-fallback for unknown/'shell', cwd-fallback to workspace if missing | Clicking a session starts its agent in the right folder. Default-active mock session is a shell (no surprise Claude usage on open); the terminal-ui session points real `claude` at this repo |
| 22 | 2026-06-09 | Test the resolver, not the PTY probe | Extracted `resolveLaunchSpec` as a pure fn (fs check injected) + unit-tested it; dropped a flaky PTY probe that fought Windows ConPTY / Electron-as-node | Agent→spec+cwd logic is deterministic and belongs in unit tests; node-pty launch is already proven by the shell-echo integration test |
| 24 | 2026-06-09 | node-pty under F5 | ConPTY hangs spawn under the debugger (node-pty#640); fall back to winpty when `inspector.url()` shows a debugger attached, keep ConPTY otherwise | Fixed the empty/hung terminal under F5; explained why the no-debugger integration test passed |
| 25 | 2026-06-09 | SessionManager decoupled from terminals | Made it a pure model store; PtyHost (host) + xterm (webview) own processes keyed by session id; removed VsCodeTerminalHost | Webview is now fully state-driven; multiple agent terminals run concurrently, kept mounted so switching never kills them |
| 26 | 2026-06-09 | Real panels | Git Changes + file tree (`projectInfo.ts`, git status/numstat + shallow fs walk) and Customizations counts (.claude + ~/.claude) sent per active project; sessions persisted | Replaced the mock right-panel/customizations with live data; preview still uses mock state so the UI path is identical |
| 23 | 2026-06-09 | Empty-terminal fixes | Reported empty/uninteractive terminal in real VS Code. Fixes: (a) `openInNewWindow` default now FALSE — moving the editor recreated the webview after the PTY started, so the new view hit the "already running" guard with no output; (b) TerminalPane now registers listeners before term:start and defers fit+start two rAFs so the container has a size (0 rows rendered nothing); (c) pty spawn errors surface into the terminal instead of being swallowed; (d) added an "Agent Deck" OutputChannel for diagnostics | Couldn't reproduce in the browser preview (which worked); these target the VS-Code-only differences. Awaiting Nima's F5 retest |
| 27 | 2026-06-09 | **Pivot: standalone Electron app** | After the VS Code extension kept fighting the webview sandbox (ConPTY hang, empty terminal, .vsix node-pty packaging), and learning Warp is now open source (Rust/GPU UI — ~0% reuse), Nima chose a standalone Electron desktop app. Transplanted ~80% of the repo: sessionManager/agentRegistry/persistence/projectInfo/ptyHost/protocol/types + the entire React webview + all 4 unit suites carry over unchanged. Replaced extension.ts/dashboardPanel.ts with electron/main.ts (owns managers + IPC) and electron/preload.ts (contextBridge). Removed @types/vscode, @vscode/test-electron, and the vscode integration test. Done on branch `electron-pivot` | The hard bugs were all costs of smuggling a PTY into a sandboxed iframe, not the feature itself. A real Electron renderer removes that whole class of problems and keeps our familiar React/TS stack, unlike forking Warp (Rust) or VS Code (oversized) |
| 28 | 2026-06-09 | IPC + persistence + config | Transport swaps from vscode postMessage to Electron contextBridge (`window.agentDeck.post/subscribe` over ipcRenderer ↔ ipcMain `to-host`/`to-webview`). globalState → JSON files in `app.getPath('userData')` (sessions.json); agent defs → agents.json with built-in defaults (claude + shell). Folder picker → Electron `dialog.showOpenDialog`; agent picker → native `dialog.showMessageBox` buttons | Protocol DTOs were already transport-agnostic so only bridge.ts + main.ts changed; native dialogs preserve the existing create-session UX without building an in-app modal yet |
| 29 | 2026-06-09 | node-pty prebuilt (no compiler) | Building node-pty for Electron's ABI needs Python + VS C++ Build Tools (absent here). Switched dependency to `@lydell/node-pty` (maintained 1.x-API fork shipping prebuilt binaries incl. Electron ABIs) via the `node-pty` npm alias — no import changes. Verified it spawns a real PTY under Electron ABI 125 | Avoids forcing a C++ toolchain install (the exact friction the pivot was meant to escape); `electron-rebuild` stays available as `npm run rebuild` |
